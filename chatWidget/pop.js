document.addEventListener("DOMContentLoaded", () => {
  console.log(chrome.storage);
  // Add event listeners and interactions for the permissions management interface here
  // You can use the chrome.storage API to store user preferences
});
